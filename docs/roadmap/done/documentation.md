# Documentation Overhaul — README + ARCHITECTURE.md

**Shipped:** v0.8.0
**Tag:** v0.8.0

- README rewrite: install paths (brew/cargo/binary)、30秒 quickstart、
  両モード (`run` / `shellenv`)、9 言語表、mise/asdf/proto/uv/sdkman/devbox/Nix 比較
- docs/ARCHITECTURE.md — Backend trait surface, orchestrator, on-disk layout,
  qusp.toml + qusp.lock schema, verification chain table
- 「qusp は何でないか」を明示 (package manager じゃない、Nix 代替じゃない)
