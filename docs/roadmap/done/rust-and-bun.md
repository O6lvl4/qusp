# Rust + Bun Backends

**Shipped:** v0.7.0
**Tag:** v0.7.0

- Rust: static.rust-lang.org tarball, install.sh の component merge を Rust で再実装
  (rustc/cargo/rust-std を unified bin/ にマージ)、stable/beta/nightly 解決
- Bun: oven-sh/bun zip, SHASUMS256.txt 検証、bunx → bun symlink mirror
- `qusp init` テンプレートに rust + bun 追加
