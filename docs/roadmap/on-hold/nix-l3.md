# Nix L3: Export Nix

**優先度:** Phase 5 (3.x)

## やること

`qusp export nix` で qusp.toml + qusp.lock から flake.nix を生成。
qusp で開発、Nix にデプロイ、というワークフローを成立させる。

これが qusp → Nix の **graduation path**。Nix 派が来るなら歓迎する設計。
